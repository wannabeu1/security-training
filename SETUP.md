# QR 접속자 기록 설정

이 페이지는 QR 접속 직후 이름 입력 여부와 관계없이 접속시간·공인 IP·브라우저 정보 등을 회사가 소유한 Google Sheet에 기록합니다. 이름은 선택 입력이며, 입력한 경우 별도 기록으로 남습니다.

## 1. 수집용 Google Sheet 만들기

1. 회사 Google 계정으로 새 Google Sheet를 만듭니다.
2. 상단 메뉴에서 **확장 프로그램 → Apps Script**를 엽니다.
3. 기본 코드를 지우고 `apps-script/Code.gs`의 내용을 붙여넣은 뒤 저장합니다.

## 2. 웹 앱 배포하기

1. Apps Script 우측 상단의 **배포 → 새 배포**를 선택합니다.
2. 유형은 **웹 앱**을 선택합니다.
3. 실행 계정은 **나**, 액세스 권한은 외부 QR 접속자가 요청을 보낼 수 있는 범위로 설정합니다.
   - 조직 정책에서 익명 웹 앱을 막는다면 Google Apps Script 대신 사내 API를 사용해야 합니다.
4. 배포 후 생성된 `https://script.google.com/macros/s/.../exec` URL을 복사합니다.
5. `config.js`의 `collectorUrl` 값을 복사한 URL로 교체합니다.

## 3. 테스트 및 배포하기

1. QR로 접속하면 `page_open` 유형의 접속 기록이 자동으로 생성되는지 확인합니다.
2. 이름을 입력한 경우에는 `name_submitted` 유형의 기록이 추가로 생성되는지 확인합니다.
3. Google Sheet에 `접속기록` 탭과 새 행이 생성됐는지 확인합니다.
   - `서버 기록시각`은 Google Apps Script 서버가 기록한 기준 시각입니다.
   - `브라우저 기록시각`은 접속자의 기기에서 생성한 UTC 시각입니다.
   - `공인 IP`는 `config.js`의 `ipLookupUrl`을 통해 조회한 IPv4 또는 IPv6입니다.
4. 페이지에서 비밀번호·인증번호 등 민감정보를 요구하지 않는지 확인합니다.
5. 변경 파일을 GitHub `main` 브랜치에 반영합니다. GitHub Pages가 해당 브랜치를 배포하도록 설정되어 있다면 수 분 내 반영됩니다.

## 운영 권고

- 훈련 공지 또는 개인정보 처리 근거에 이름·IP·접속 시각의 수집 목적, 보관 기간, 접근 권한을 명시하세요.
- 열람 권한은 담당자 최소 인원으로 제한하고, 훈련 종료 후 정한 기간에 기록을 삭제하세요.
- 회사 NAT, 프록시, VPN 또는 이동통신망을 사용하면 여러 직원이 같은 공인 IP로 표시될 수 있습니다. 공인 IP만으로 개인을 확정하지 마세요.
- 기본 IP 조회는 외부 서비스인 ipify를 사용합니다. 회사 정책상 외부 조회가 허용되지 않으면 `config.js`의 `ipLookupUrl`을 사내 API로 교체하세요.
- 실명 대신 사번 또는 QR별 무작위 식별자를 사용하면 오입력과 동명이인 문제를 줄일 수 있습니다.
- 직원별 고유 QR을 만들 수 있다면 `?id=무작위값` 방식이 이름 직접 입력보다 정확합니다. 단, URL에 실명·사번을 그대로 넣지 마세요.
